import { error } from "../utils/format.js";

/**
 * fihris publish — Publish an agent to the OpenFihris registry.
 * Reads the agent card from the current directory and publishes it.
 */
export async function publishCommand(): Promise<void> {
  // TODO: Implement in PR feat/cli-publish
  error("Publish not yet implemented. Coming in the next release.");
}
